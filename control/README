Run the config_testbed.bash to setup/config the testbed, run it only once.
Then change to the automating_experiment directory, and run the read_parameter_space.bash there, which will generate the dataset based on the parameter_space.


