eplb pplb4s
1 1
tcp udp
7:3 4
cbr vbr bursty
10
10
1 2:3 4
topo1

Note: All text below the empty line above is used as comments/annotations:
The process of the line is based on its line number:
1. The first line is application
2. The second line is configuration of each app
3. ...

List of control app: routing plb eplb fw pplb4s pplb4d

#!/bin/bash
#APPS={"arpcache" "routing" "plb_flexible" "SBEpLB_flexible"}
APPS plb eplb routing # Control applications, can specify 2, 3, 4 or 5 control apps here
APPCONF 2 3 1 # APPCONF, first app has 2 configurations (which will then be expanded as config 1 and config 2), second app has 3 configurations (will be expanded as config 1, config 2, config 3), third app has 1 configuration: config 1
TRANS tcp udp # TRANS
TARGETSW 2 3 : 1 : all # first app is deployed on switch 2 and 3, second app on switch 1... TARGETSW can be *all*, i.e., an app can be deployed on all switches, e.g., routing.
EPTRAFPROF cbr vbr bursty # End-point Traffic Profile
NUMSW 10 # Number of SW
NUMEP 10 # Number of end-points
EPCOMBI 1 2 : 3 4  # First field is sources, second is sinks
topology used, e.g., topo1

# APPORDER=() # generated by APPs, but actually cannot be really control when
	# starting app as part of the controller. (controller built-in app)
TOPOS 1 2 # TOPOS
PRIORITY # also generated by APPS
